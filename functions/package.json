{
  "name": "capstone-cloud-functions",
  "description": "Cloud Functions for Firebase - notifications for MangoSense app",
  "version": "1.0.0",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "echo no lint configured",
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.2",
    "nodemailer": "^6.10.1"
  }
}
